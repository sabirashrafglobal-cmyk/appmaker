name: reldummy
description: A dummy industrial mechanical reliability analysis app with KPIs
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  charts_flutter: ^0.12.0
  flutter_bloc: ^8.1.2
  intl: ^0.17.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
